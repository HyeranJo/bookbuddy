import { styled } from 'styled-components';

export const Styled_CSDeleteModal = {
  Buttons: styled.div`
    display: flex;
    width: 100%;
    justify-content: space-around;
    margin-top: 20px;

    & > button {
      width: 50px;
    }
  `,
};
