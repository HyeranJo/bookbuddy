import { styled } from 'styled-components';

const Styled_Input = {
  Input: styled.input`
    width: 415px;
    height: 60px;
    font-size: var(--input-font-size);
    padding: 10px;
  `,
};
export default Styled_Input;
